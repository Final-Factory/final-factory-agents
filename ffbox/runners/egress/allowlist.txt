# ffgithubrunners egress allowlist — the ONLY hostnames a job may reach.
#
# One name per line. A bare name matches exactly; a leading "*." matches any subdomain of it (and
# not the bare domain — list that separately if it is needed). Blank lines and # comments are
# ignored. Anything absent is refused at the SNI, with the attempt logged.
#
# THIS IS NOT ffbox's LIST, and the difference is the point. ffbox's has no GitHub entry at all,
# deliberately: its container never pushes, the host does. CI has to reach github.com, the Actions
# broker, LFS, artifact storage and the cache service, and putting those on ffbox's list would
# hand ffbox's containers a reach they do not have today. Two instances of the same script, two
# lists.
#
# Nothing Anthropic is here. A job runs a workflow, not an agent.
#
# docs/egress.md is the reference: how a name is decided, the two ways one is refused, and why a
# refused host can leave no trace in the SNI log.
#
# HOW TO ADD SOMETHING. Do not guess. Put the proxy in log mode for a few real jobs —
#     FFBOX_EGRESS_MODE=log sh ffbox/runners/03-image.sh --egress-only
# — which permits everything and records every destination, then read the SNIs back out:
#     sh ffbox/runners/03-image.sh --egress-log
# Add what the jobs actually asked for, restart in enforce mode, and confirm nothing new appears.
#
# THE IMAGE BUILD IS NOT BEHIND THIS FENCE. `docker build` uses the daemon's default network, so
# pulling the base image, the runner tarball and gh is unfiltered. Nothing build-time belongs
# here, and a failing build is never a reason to add a line.

# REMOVED 2026-08-31: github.com, api.github.com, github-cloud.githubusercontent.com.
#
# All three went because a job stopped NEEDING them, not because they were filtered. That is the
# difference that matters: an SNI fence cannot restrict github.com to one repository -- the repo
# lives in the URL path, inside the TLS -- so while any of these were reachable, hostile test code
# could push to its own repo or POST to /gists with a credential of its own. Measured from a live
# runner while they were still listed: api.github.com/repos/torvalds/linux answered 200 and
# /gists answered 200.
#
#   github.com + github-cloud   The repository and its LFS objects now come from ffghr-gitmirror
#                               on this network: git daemon for objects, a download-only LFS batch
#                               server beside it. The runner image rewrites the GitHub URL with
#                               url.<mirror>.insteadOf, so actions/checkout never knows. There is
#                               no repository but ours at the other end, so "restricted to our
#                               repo" is a property of what exists rather than a rule to maintain.
#   api.github.com              Used by NEITHER the runner NOR any action -- only by our own two
#                               scripts. post-check-run.py now hands its payload to the supervisor,
#                               and mode2Freshness moved to ubuntu-latest, off this fence entirely.
#
# WHAT BREAKS IF THE MIRROR IS DOWN: every job, immediately and loudly, because there is no longer
# a fallback. That is deliberate -- a silent fallback to GitHub is exactly what hid the mirror
# serving only master for a day -- but it makes ffghr-gitmirror load-bearing. It is
# --restart unless-stopped and 03-image.sh brings it up.

# --- GitHub: the runner itself ------------------------------------------------------------------
# CONFIRMED by a real job on 2026-08-29. The wildcard earned itself: one job touched seven
# distinct hosts under it — broker., pipelinesghubeus14., token., run., launch.,
# results-receiver. and run-actions-3-azure-eastus. — which no hand-written list would have
# anticipated. Do not try to enumerate these.
*.actions.githubusercontent.com

# NOT yet seen from a job. main.yml's cache-pruning step calls gh, which needs it; the smoke
# workflow has no such step, so this stays on the list unconfirmed.
# --- GitHub: what a job downloads ---------------------------------------------------------------
# REMOVED 2026-08-31: codeload.github.com and objects.githubusercontent.com.
#
# codeload was never the repository fetch, which is what its old comment here claimed. It was the
# runner downloading the CODE FOR EACH ACTION before step one -- visible on every job including
# mode2Freshness, which checks out almost nothing. The image now carries those archives
# (ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE) and a real job no longer touches codeload at all.
#
# objects. carried no traffic in 27 hours of jobs.
#
# SAFE BECAUSE main.yml IS THE ONLY WORKFLOW BEHIND THIS FENCE. activation.yml is on
# ubuntu-latest, deploy.yml on self-hosted, determinism-nightly.yml on finalfactory-mode2-windows.
# main.yml's two jobs use actions/checkout and actions/upload-artifact and nothing else, both
# pinned by SHA and both in the image cache.
#
# THE FOOTGUN, since this is now a thing that can break a build: adding a new `uses:` to main.yml
# without adding it to the cache in ffbox/Dockerfile means the runner tries to download it, is
# refused here, and the job fails before step one. That is the intended trade -- a loud failure
# rather than a quiet reach -- but it is the failure to expect.

# --- Git LFS --------------------------------------------------------------------------------------
# CONFIRMED 2026-08-29: actions/checkout with lfs: true smudges through git-lfs filter-process,
# and the objects came from github-cloud.githubusercontent.com. github-cloud.s3.amazonaws.com was
# a guess and went untouched, so it is gone: an allowlist entry nothing uses is only extra reach.
#
# NARROWED 2026-08-31, which is what "once a few more jobs agree" was waiting for. Over 27 hours
# of real jobs the bare wildcard earned exactly two names that *.actions.githubusercontent.com
# above does not already cover:
#
#   github-cloud.githubusercontent.com    LFS objects. Kept, below.
#   copilot-proxy.githubusercontent.com   Reached 9 times and ALLOWED. Nothing in main.yml asks
#                                         for it. Dropped: an allowlist entry nothing needs is
#                                         only extra reach, and this one accepts requests.
#
# So the wildcard bought one wanted host and one unwanted one. Two exact entries instead.
# --- actions/cache and actions/upload-artifact ----------------------------------------------------
# CONFIRMED 2026-08-29: upload-artifact went to productionresultssa5.blob.core.windows.net. Sixteen
# distinct shards have been seen since — sa0, 2, 4-14, 16-18 — which is why this cannot be a list.
# main.yml restores a Library cache and uploads two artifacts on every job, so if this were wrong
# the failure would be loud rather than subtle.
#
# A REGEX AND NOT `*.blob.core.windows.net`, and this is the most important line in the file.
# blob.core.windows.net is a MULTI-TENANT namespace: an Azure storage account name is claimed
# first-come by anyone with a free account. The wildcard therefore permitted `<attacker>.blob.
# core.windows.net` — an open, unauthenticated, high-bandwidth path out of the fence that had
# nothing to do with GitHub and would not have been closed by any amount of GitHub filtering.
#
# Contrast *.actions.githubusercontent.com and *.githubusercontent.com above, which stay wildcards
# and are fine as wildcards: GitHub owns those DNS zones, so no third party can put a name under
# them. Vendor-controlled namespace, safe. Customer-controlled namespace, not.
#
# Non-matching names under the suffix still resolve here and are refused by nginx, so an attempt
# lands in the SNI log with the name it asked for. See docs/egress.md.
~^productionresultssa[0-9]{1,2}\.blob\.core\.windows\.net$   blob.core.windows.net

# --- CANDIDATES FOR REMOVAL, 2026-09-01 ------------------------------------------------------------
# The licence became an offline .ulf on this date (ffbox/unity-offline-license.sh), and the offline
# resolver makes NO call to Unity's licensing service. So the ACTIVATION half of the block below --
# license.unity3d.com, activation.unity3d.com, api.unity.com, id.unity.com, accounts.unity3d.com --
# should now carry no traffic at all.
#
# NOT DELETED YET, ON PURPOSE, and this file's own rule is why: "Do not guess. Put the proxy in log
# mode for a few real runs." Do that, confirm those five are silent across real runs, and then drop
# them. An entry nothing uses is only extra reach.
#
# KEEP core.cloud.unity3d.com AND public-cdn.cloud.unity3d.com REGARDLESS. The licensing client
# fetches public-cdn.cloud.unity3d.com/config/production on every start even with a valid local
# licence -- observed directly on 2026-09-01 -- so that one is load-bearing on the offline path too.
# --- Unity licensing --------------------------------------------------------------------------------
# CONFIRMED 2026-08-29 by a real activation that succeeded on the first attempt and returned its
# seat. Four of these carried traffic: license.unity3d.com, activation.unity3d.com,
# core.cloud.unity3d.com and public-cdn.cloud.unity3d.com. The accounts/id/api entries are the
# login leg of the -username/-password flow and stay listed even though this activation did not
# need them.
#
# What unity-license.sh needs to activate a seat and hand it back.
license.unity3d.com
activation.unity3d.com
api.unity.com
id.unity.com
accounts.unity3d.com
core.cloud.unity3d.com
public-cdn.cloud.unity3d.com

# --- Unity Package Manager ----------------------------------------------------------------------------
# A job restores Library/ from the actions cache, so UPM should resolve from that and never come
# here. Listed because the cost of being wrong is a failed import well into a 90-minute job.
#
# download.packages.unity.com was REFUSED on 2026-08-29 and added as a result. It is also a worked
# example of the second refusal path in docs/egress.md: the bare entry `packages.unity.com` made
# dnsmasq resolve the subdomain by suffix, so it reached nginx, whose match is exact, and landed in
# the deny sink with a logged sni= line. A name with no matching suffix would have died at DNS with
# no sni= line at all. The activation still succeeded, so this was not fatal here — but a cold UPM
# resolve in a real import is exactly where it would be.
packages.unity.com
cdn.packages.unity.com
download.packages.unity.com
