---
name: project-memory
description: Accumulated Final Factory project memory — standing user feedback plus hard-won Unity, Burst/ECS, baking, modding, and play-mode lessons. Consult at the start and end of every Final Factory repository development task so fixture-selection and the required @lothsahn Discord completion notification are not missed; also consult BEFORE debugging the MCP bridge, running tests, entering play mode, editing open scenes, diagnosing Burst NREs after a merge, or working on mod loading.
---

# Final Factory project memory

One file per lesson under `memories/`. Read the entry for the area you are touching before
re-deriving anything the hard way. These were accumulated across the develop and master
worktrees and apply to ALL branches.

## Standing user feedback (always applies)

- [Context relay at ~65% and the network-tutorial goal](memories/feedback-context-relay-and-network-tutorial-goal.md) — Ben 2026-09-11/12: at ~65% context run /ff-agents:handoff then open a FRESH Claude Code instance on the same Mac via osascript Terminal + `claude --remote-control "M5 FF<n>" "/ff-agents:resumeFromHandoff"` (numbered in relay order, never compact); standing goal = the Hand-Hold tutorial across the network (M5 windowed HOST, BEAST headless CLIENT) with two agents as real players, zero desyncs, fix as you go; no Lothsahn FYI needed
- [Prove multiplayer work over live networked machines](memories/feedback-prove-over-live-networked-machines.md) — Ben 2026-09-11: a determinism/MP fix is proven only by a live networked leg between machines on his network (BEAST Windows host / Mac peer), never by an editor pair on one box; put the cross-machine leg in the plan before claiming proven
- [Use standard test commands](memories/feedback_test_command.md) — run tests via MCP `run_tests`/`get_test_job` (pinned instance); for remote Pipeline access, use the public MCPForUnity API through project-scoped `eval_file` for exact assembly filtering; the file-trigger channel is retired for routine runs
- [MCP bridge unavailable = report and recover](memories/feedback_mcp_bridge_down_stop.md) — discover Unity MCP in either runtime and pin by project path; if unavailable, report the gap and recover only the exact project-owned editor per editor-ops, never silently substitute file evidence or kill broad Unity process groups
- [Real upgrade steps over fast loads](memories/feedback-upgrade-steps-over-fast-loads.md) — Ben 2026-09-12: a real [Save] layout change gets an UpgradeStep with RequiresEntityDictionary true and older saves load slowly once; fast loads only matter when the save version matches the build; re-mint a fast-path golden fixture instead of weakening the step
- [No cd prefix in Bash](memories/feedback_no_cd_prefix.md) — never prefix commands with `cd` into the working dir; it triggers approval prompts, use paths directly
- [Drive interactive verification myself](memories/drive-interactive-verification.md) — standing authorization for game launches, gameplay, screenshots, and scoped recovery; no repeated app-use questions; bounded calls and peer cleanup prevent unattended stalls
- [Paired capture and helper ownership](memories/paired-diagnostic-capture-and-helper-ownership.md) — checkpoint both peers early to prove positive diagnostic capture; in three-peer diagnostics join the investigated client last; late arming can miss a whole join block; delegated peers need separate helper and output filenames in the shared workspace
- [Watch logs without full scans](memories/watch-logs-without-full-scans.md) — never poll a big log by re-scanning it each tick (the watcher falls behind and stalls silently); query once directly first, then tail/byte-offset only
- [Skill docs go decision-first](memories/skill-docs-decision-first.md) — when a doc fails to stop a mistake, restructure it: falsifying test first, look-alike traps contrastive, recipes to a companion file
- [Choose the smallest representative playtest fixture](memories/feedback-focused-fixtures-before-meltcpu.md) — default to Wittle Base, FlatMap, or another focused 16-UPS fixture for behavior/visual/feel tests; reserve MeltCPU for explicit worst-case scale and performance questions
- [Notify Lothsahn when a repository task finishes](memories/feedback-discord-completion-notify-lothsahn.md) — ONLY when running in ffbox (never in an interactive developer-machine session, Ben 2026-09-12): after successful implementation and verification, post one extremely brief `@lothsahn` FYI in Discord `#dev-chat`, in PLAIN ENGLISH saying what behavior changed (no hashes/system names/audit stats); batch related substeps into one completion ping
- [Publish harness changes/lessons to ff-agents proactively](memories/feedback-publish-harness-changes-to-ff-agents.md) — whenever a session changes shared harness behavior or learns a durable, reusable lesson, run the publish-skills workflow in that SAME session without being asked; batch the session's lessons into one version bump; report a blocked publish explicitly
- [ffbox installs as ONE service](memories/ffbox-installs-as-one-service.md) — every ffbox machine gets all of it (harness + Discord pipeline + ffweb) via `sh ffbox/setup.sh`, which installs AND starts `ffbox.target`; the `--skip-*` flags are re-run controls, not a menu, and a setup script must never stop one step short of running
- [The ffbox updater restarts everything](memories/ffbox-updater-restarts-everything.md) — `ffbox-update.timer` fires ~every 5 min, fast-forwards the checkout, runs setup.sh stage 5 (`registerAgents.sh`, which is how a plugin bump lands) and restarts the whole `ffbox.target` including the Discord listener; it drains BOTH lanes first, destroys idle containers and STANDS DOWN rather than killing one with work in it, so a push can legitimately wait behind a CI job — after a push WAIT AND CHECK THE JOURNAL rather than restarting anything by hand; only systemd UNIT FILE changes need a human — since 2026-09-14 every pass also re-applies the egress fence (`ffbox-egress.sh up`, deferred while a fenced run is in flight), so an allowlist edit lands on its own
- [ffbox/config.md is the settings reference](memories/ffbox-config-md-is-the-settings-reference.md) — `~/.config/ffbox/config.json` carries VALUES ONLY, with no `_help` blocks in it (stage 5 deletes any left over). Every key is documented in `ffbox/config.md`, and a change to the config's shape (`05-discord-setup.sh`'s seeded template, ffwatch's `DEFAULTS`/`ENV_OVERRIDES`/`load_config`, `runners/lib/config.sh`, or `ffbox`'s `container` reads) belongs in the SAME commit as the doc edit; never put help text back into the JSON
- [Unity licensing is ONLINE activation, host-side](memories/unity-personal-licence-is-activated-online.md) — "offline" in ffbox names only the RUN CONTAINER's view; Unity Personal CANNOT be activated offline (manual .alf activation was withdrawn for Personal in Aug 2023). The HOST activates via a throwaway container running `--activate-ulf`, producing ONE `.ulf` bound to the pinned machine id `46696e616c466163746f72792d666662` that every slot and both lanes share — not one per slot. It carries a rolling ~24h `UpdateDate`, so `unity-offline-license.sh ensure 4` re-activates before a container launch when under 4h remain (no timer exists). Check `unity-offline-license.sh status` before claiming anything
- [Two docker daemons on the build server](memories/ffbox-two-docker-daemons.md) — everything real runs on `/run/ffbox-container/docker.sock` while this account's docker CONTEXT still points at `/run/user/1015`; both can hold containers and networks with the SAME NAMES, so a bare `docker` command succeeds against the wrong machine and says nothing about it. Set DOCKER_HOST, and verify a fix through the path that uses it rather than the handle that applied it
- [Capture a long command's output once, don't re-run it per grep](memories/feedback-capture-output-once-not-per-grep.md) — redirect a slow command/test suite to a task-unique scratch file in the WORKING dir, grep that file, then delete it; agents share these machines under one user, so `/tmp/out.txt` is not yours and `git add -A` will commit what you leave behind
- [Never print a secret file into the transcript](memories/feedback-never-print-a-secret-file.md) — never read `secrets.env`, `~/.git-credentials` or the like into a tool result: not the value, not a prefix, not a redacted line. Answer "is it set" with `grep -c '^NAME='` or `[ -n "$NAME" ]`; same rule for bare `env`, `docker inspect` (`Config.Env`), `systemctl show` and `journalctl`
- [Write status reports in simpler, plainer language](memories/feedback-simple-report-language.md) — Ben wants the TL;DR-first shape and full detail kept, but carried by plain words and short sentences; run ids and codenames go in parentheses or bullets, never woven into dense prose
- [Don't hand Ben steps the harness already does](memories/feedback-no-manual-steps-the-harness-already-does.md) — before writing a "your steps" list, READ the automation for each line; on ffbox the updater already runs `registerAgents.sh` and restarts the target, so those are a sentence about timing, not tasks. Only unit-file installs, the egress allowlist and off-machine actions (a Discord permission grant) are his
- [launch-editor.sh brings editors up Burst-DISABLED](memories/launch-editor-burst-off.md) — always editor-preflight after a relaunch, enable + drain to a stable zero (two readings 60s apart) before paired work
- [Headless leg died mid-task: corrective resume](memories/headless-leg-corrective-resume.md) — pull the sid from the leg log, rewrite the prompt file with a corrective nudge, run-leg.sh <leg> <sid> <model>; context survives sleep-deaths and end-turn-to-wait deaths; PREVENTION — a headless leg ends at its final message, never wait on a Monitor/background task, foreground bounded waits only
- [code-review-sonnet synthesis retries: workaround](memories/code-review-sonnet-synthesis-retry-workaround.md) — if the synthesis stage exhausts 5/5 retries on its `decisions` schema, don't chase the schema (it's a per-session generated script, not a repo file, and does declare the field) — synthesize by hand from `journal.jsonl` instead
- [Low battery during orchestration: wrap up and sleep](memories/low-battery-wrap-up-and-sleep.md) — Ben (2026-09-04, after the M5 laptop died mid-orchestration and slept for five hours with legs and monitors in flight): if the battery is getting low, WRAP UP AND SLEEP THE MACHINE — commit/push what's verified, write the handoff, log the queue file, then `pmset sleepnow`. Check `pmset -g batt` before every long lane and leg launch; `~/ff-worker/run-leg-local.sh` refuses to launch under 25% battery. Editors/bridges survive a sleep, but headless legs, monitors and cross-machine runs do not — a run interrupted by sleep is a lost run, not a failed one

## Unity MCP bridge

- [Unity MCP server](memories/unity-mcp-server.md) — Coplay MCP-for-Unity via stdio (multi-instance safe); Claude spawns server, editor must be set to Stdio transport in Window>MCP for Unity; tools load only at Claude restart
- [Unity MCP resource URIs](memories/unity-mcp-resource-uris.md) — resource URIs use slashes and differ from the `name` field; editor readiness is `mcpforunity://editor/state` NOT `editor_state`; read the `uri` field, don't build from name
- [UnityMCP per-project config](memories/unitymcp-per-project-config.md) — the Unity bridge tools require a per-project UnityMCP server in ~/.claude.json (claude mcp add + restart)
- [Verify compile via DLL string check](memories/verify-compile-dll-string-check.md) — refresh_unity can compile before a just-written Edit lands or skip compiling entirely (refresh_triggered false); confirm edits are live via DLL mtime, a new UTF-16 literal, or the live method's IL
- [A git-deleted `.cs` under a live editor: CS2001 + silent stale assembly](memories/git-deleted-cs-under-live-editor-cs2001.md) — after a checkout/revert removes a script, `refresh_unity scope=scripts` says `refresh_triggered:false`, the compile fails with `error CS2001 … could not be found` and the OLD assembly stays loaded while the state reads idle; use `scope=all mode=force`, then confirm a newer domain reload AND the deleted type's absence by reflection
- [Unity Editor.log gotchas](memories/unity-editor-log-gotchas.md) — **the log is SHARED by every running editor, so nothing in it is evidence about YOUR pinned instance** (only MCP job ids / pinned `read_console` are — same rule as the shared TestResults.xml); line numbers unreliable (binary content), slice with `awk` after a dropped marker, `grep -a`, map bridge port→PID to find the right editor, saturated main thread freezes the bridge without hanging the editor
- [Machine-global state across checkouts & sessions](memories/machine-global-state-multi-session.md) — **LocalLow `Never Games/finalfactory/` (saves/, DeterminismAudit/, TestResults.xml) is keyed by product name, shared by EVERY checkout and every concurrent Claude session**; local-host UDP ports 7777+ are ALREADY auto-negotiated on develop (explicit config port = a paired-run contract that fails loudly; otherwise probe upward past taken ports — so **never assume a checkout is on 7777**, and a `Failed to bind UDP socket` error is now a regression, not contention); MCP pins are per-session but subagents share the parent's pin; never rename/clear the shared dirs while a neighbor session may be live; close every editor you open (four editors + load loops = OOM)
- [Editor via the `unity` CLI when the bridge is down](memories/editor-via-unity-cli-when-the-bridge-is-down.md) — `~/.local/bin/unity command --project-path <repo> …` is a full substitute for a CONNECT_TIMEOUT bridge: recompile/recompile_status, eval/eval_file (Roslyn statements, no `using`, `--file`), editor_play/stop, console logs, run_tests/test_status, and a guarded EditorApplication.update callback for MP-dev builds with a marker file; wait for the ItemConfig singleton before LoadGame; ignore a bundle's stale status file

## Editor & play mode traps

- [Play mode needs main scene](memories/playmode-needs-main-scene.md) — entering play mode hangs forever unless Assets/Scenes/main.unity is the active scene; check editor_state.active_scene first; a FRESH editor boot with no scene open instead looks wedged as an empty ~26-entity world under EditorApplication.Step()
- [No external edits to open Unity scenes](memories/no-external-edits-to-open-unity-scenes.md) — Write/Edit on an open .unity/.prefab raises a modal reload dialog that blocks the main thread and hangs the MCP bridge; edit via SerializedObject + SaveScene instead
- [Unity keyword-remap shader crash](memories/unity-keyword-remap-shader-crash.md) — known Unity engine bug crashing import worker on ParticlesUnlit fallback keyword remap
- [wsay voice notifications](memories/wsay-voice-notifications.md) — global hooks speak via wsay when Claude finishes (Stop) or needs input (Notification); don't disable
- [Profiling ECS + fork pin bumps](memories/knn-profiling-and-fork-pin-gotchas.md) — Unity 6000.3.19f1/game revision `7d696256c` can record exact main-thread system markers with a qualified `ProfilerRecorder` recipe; samples are inclusive diagnostics, not additive production budgets; preserve the `ProfilerDriver` fallback; clone Packages/ can be a real dir: copy manifests + Client.Resolve() on the clone, verify PackageCache sha + fresh assembly mtime before trusting any run
- [Paired-leg preflight and editor wedges](memories/paired-leg-preflight-and-editor-wedges.md) — both editors must pass editor-preflight.sh (Burst enabled+drained, re-enable it first: the launcher leaves it off) before any leg, AND the first session after a forced recompile is a cold-Burst-cache warm-up whose forks are not the game's (074); the SIGBUS crash, the no-live-World wedge, the scene-changed-on-disk modal after a pull, the fresh-mtime rule for writing the client config, OutlivePairBreak + reclaim-identity files for rejoin legs
- [Build after the fast suite overwrote main.unity; git checkout of an open scene wedges BOTH editors](memories/build-after-fast-suite-overwrote-main-scene-and-open-scene-checkout-wedge.md) — the EditMode runner leaves its empty scene active and `BuildCommand2` saved it over `Assets/Scenes/main.unity` (guarded since 80386cea4 — still check the active scene before scheduling a build); restoring the file on disk while an editor had it open wedged the main AND clone editors (worktree symlink) on the native modal — shim + bridge time out, ~1% CPU; kill + `scripts/launch-editor.sh`

## ECS / Burst / baking

- [Stale Burst after merge](memories/stale-burst-after-merge.md) — check `EnableBurstCompilation` before EVERY test run (Burst off = a green suite that never exercised the Burst compile, plus ~10x slower); Burst compiles ASYNCHRONOUSLY: a clean console right after `refresh_unity` proves nothing, wait for `BurstLoader.BurstProgressId` to go idle before reading results; a current managed assembly and fresh domain reload can still execute stale native JIT, so diagnose loaded IL and behavior, preserve the exact isolated editor's `Library/BurstCache/JIT` before restarting, then re-run with Burst enabled and drained; a standalone crash stack with only a method name → reproduce with Burst OFF for the full managed stack
- [Bursted job layout vs domain reload](memories/bursted-job-layout-not-domain-reload-invalidated.md) — a job struct-layout change surfacing as `UNKNOWN_OBJECT_TYPE ... has not been assigned` (distinct symptom from stale-burst-after-merge's NRE/BC1054) needs the `EnableBurstCompilation` off/on TOGGLE, not just a domain reload; a nested `ComponentLookup` job field must never be `default`, even when unused
- [ECS exception class matching](memories/ecs-exception-class-matching.md) — match the exception CLASS before theorizing: ECB append on a missing buffer → `InvalidOperationException "Buffer does not exist"`; `SetComponent`/`GetComponentData`/lookup indexer on a missing component → `ArgumentException`
- [Short-circuit hoist unguards a lookup](memories/short-circuit-hoist-unguards-lookup.md) — hoisting a read out of a `&&` can silently remove the guard it depended on (T044j → WanderSystem AncientGuardian crash); check whether the short-circuit was load-bearing before hoisting
- [`ElapsedGameTime` was the per-peer wall clock](memories/elapsedgametime-is-the-per-peer-wall-clock.md) — retired in-repo (073 T022): use `SimulationElapsedTime` / `WallClockElapsedTime`; the comet spawner forked on the roll seed AND on the player query's entity order; full reader census in Documentation/RealElapsedTime-Determinism-Audit.md.
- [Per-peer gates on structural changes fork the census](memories/per-peer-gates-on-structural-changes-fork-the-census.md) — one class, three finds in a day (073): a fixed-group system instantiates / tags / links behind the LOCAL camera (inserter route displays, T027), a player's presentation `LocalTransform` + one unsaved shared position (breadcrumbs — 26,464 in hazel1831, T028), or `MePlayer` (`ValidForPlayerAbility` on Defense Platforms and their ships, T029 — fixed by keeping the tag local, stripping it from the census and giving its one simulation reader a per-player record); grep recipe inside; census red on EVERY heartbeat after a recovery = an unsaved per-peer tag on live host entities
- [Station-grid mailboxes must share one holder](memories/station-grid-mailboxes-must-share-one-holder.md) — `StationGridUpdateCommand` is a per-PLAYER mailbox drained per entity in chunk order; `PlaceableDeletionSystem` posted to MePlayer while `StationConnectionsSystem` posted to HostPlayer → one buffer on the host, two on a client → a SwapItem's Remove+Add resolved to different grids (073 T031, fixed `cba0dc6fd`); every producer posts to HostPlayer until #247. Found by an IDLE no-chain control leg (clean) + a walk-only leg (forks): when a fork sits at a fixed offset from both the join and the chain, run the no-action control first
- [Marker consumers must clear the whole station in one heartbeat](memories/marker-consumers-must-clear-the-whole-station-in-one-heartbeat.md) — a `[Save]`d work marker set on ~30 structures at once and cleared ONE per Execute drains one per heartbeat in chunk order = per peer (073 T032 `RecalculateHaulersMarker`, 4,476 red hb while SwapItem upgrades kept refilling it; fixed `6384314c5`); the tell is equal-cardinality membership differences in `CensusDetail`; diagnose with a per-heartbeat marker timeline over the reports you already have before scheduling another leg
- [A fixed-group ECB must play back later in the SAME engine frame](memories/fixed-group-ecb-must-play-back-later-in-the-same-frame.md) — recording into `FFEarlyBufferSystem` from a group AFTER its playback (PreTransform onward) lands the change at the start of the NEXT ENGINE FRAME, and whether that frame carries a heartbeat is per peer, so every FixedEarly reader sees it one heartbeat apart (073 T033: SwapStation's replacement structure, six one-heartbeat `census` blips; fixed `1174e30bd` via `FFLateBufferSystem`, leg g13 census-equal on 5,189 hb); defer WITHIN the frame, never across it; one cross-frame recorder left: `HealthBarSystem`
- [A fixed-group read of LocalToWorld is a frame-count bug](memories/fixed-group-reads-of-localtoworld-are-frame-count-bugs.md) — `LocalToWorld` is written once per ENGINE FRAME by `TransformSystemGroup`, so a heartbeat-cadenced reader's value depends on how many frames elapsed (055 R37: a late-group spawn entered the KD-tree at the prefab matrix on one peer only); fix it at the READER by deriving what the transform pass would compose — mirroring its `Parent`/write-group exclusions — never per writer. Now GUARDED by `FixedGroupLocalToWorldReaderGuardTest` (reader-side reflection+source census of every pre-pass fixed-group reader)
- [Fixed-group `SystemAPI.Time.DeltaTime`/`ElapsedTime` reads are frame-rate desyncs](memories/fixed-group-engine-time-reads-are-frame-rate-desyncs.md) — inside a fixed group they are the ENGINE frame delta / wall clock (the controller group pushes time; the fixed group only sets `FFTimeData.deltaTime`), so a simulation-visible accumulator forks by fps (073: a 20 fps peer deleted an exhausted asteroid ~1150 hb before a 240 fps peer, proven on built Mac↔Windows players; fixed `800974582`). Read `FFTimeData.deltaTime`. A reader that only writes presentation (LogisticsBay animation children) is the same smell, not a desync — trace the write's consumers
- [Burst job helper structs must be public](memories/burst-job-helper-structs-must-be-public.md) — a struct held as a field of (or returned by) a `public` job struct must itself be `public`, or CS0052/CS0050 "inconsistent accessibility" — and if that's on disk at editor boot, it lands in Safe Mode (see the editor-ops skill); default new job-helper structs to public
- [Test-assembly systems need [DisableAutoCreation]](memories/test-assembly-systems-need-disableautocreation.md) — unmarked SystemBase/ISystem in Assets/Tests auto-creates into live play mode (static entities accumulate render-only drift the fingerprints can't see); always mark; diff LtW vs LocalTransform to detect
- [FFSystems/Player namespace collision](memories/ffsystems-player-namespace-collision.md) — systems in FFSystems/Player/ MUST use namespace `FFSystems.Players` (plural); `FFSystems.Player` collides with the Player component type and breaks all of FFSystems
- [ItemEntities baking pipeline](memories/itementities-baking-pipeline.md) — Resources/ItemEntities/*.prefab bake via EntityPrefabContainerBaker; author companion entities with CreateAdditionalEntity(ManualOverride) → in LinkedEntityGroup, remapped on Instantiate
- [ECS runtime material unload gotcha](memories/ecs-runtime-material-unload-gotcha.md) — runtime new Material() in a RenderMeshArray needs HideFlags.HideAndDontSave or UnloadUnusedAssets nulls it (BRG MaterialID <null> error)
- [BlockAllocator budget crash](memories/blockallocator-budget-crash.md) — "Cannot exceed budget of 16777216": the two fixed 16MB allocators, what actually grows them (distinct-archetype count, NOT repeated identical CreateEntityQuery), and the save-load one-at-a-time AddComponent archetype explosion + ComponentTypeSet batching fix
- [Unscoped marker sweeps fork multiplayer](memories/unscoped-marker-sweeps-fork-multiplayer.md) — a query over "every entity with kind-marker X" mutates other peers' preview ghosts (4 instances on BlueprintItemMarker); scope by BlueprintGhostOwner IDENTITY (never timing), fail closed on a missing stamp, and check shared-hash queries for peer-local matches via BAKED components (grep the prefab, don't trust "structure-only")
- [055 liveness-witness and comparison-surface lessons](memories/055-liveness-and-comparison-surface-lessons.md) — a liveness exit code alone is vacuous without the decline/mint audit record (R31); a liveness failure behind a real divergence is unexplained, not vacuous, until the divergence is fixed (R31); liveness dwells must anchor on heartbeats not wall-clock — HeartbeatSystem applies ≤1 hb/engine-frame, so UPS is FPS-capped (R32); comparison queries must match producer `EntityQueryOptions` or silently drop every Disabled entity (R34); host-only unsaved `[Save]` gaps (incl. the zero-size-tag no-op trap) fork on any reload, not just a join (R33); archetype-ordered KD-tree input makes neighbour SETS order-dependent — split every topology digest into ORDER vs SET variants (R34b); the R35 fix closes that fork at the KD-tree's BUILD INPUT via a canonical identity key + a bounded serial sort, never by serializing the hot job — plus naming the key's non-injective residue, flipping the roster instrument's assertion once its order became canonical, forbidding cross-seam key reuse (C3 vs legacy), and treating a sub-variance perf delta as a pooled-statistic claim only (R35); an end-of-heartbeat fingerprint can't adjudicate a mid-heartbeat consumer — digest the ACTUAL retained build buffers, not a re-query, and a sub-hash that folds only fields that can't differ is structurally blind (R36); rank hypotheses THEN build the discriminating instrument before picking a fix (R36→R37)
- [Join/load-route provisioning is a desync class](memories/join-load-route-provisioning-desync-class.md) — seven 055 forks, one shape: a load-time applier one group too early (R22), structural-change-inside-a-ComponentLookup-loop + paused-world systems silently no-op (R22b), a per-player component installed on only SOME arrival routes (R24), an allocator advancing by RESERVED instead of WRITTEN ids (R26), a controller-group write feeding a fixed-group read (R27), a "heal on load" repair pass that must decline via `AppliesRemoteAuthoritativeWorld` on a remote snapshot apply — even when the mint lives outside a named migration system, e.g. straight in `SaveGameManager` (R28/R29/R31), and a not-`[Save]`d prefab-spawned entity feeding an early-group consumer before its own positioning system runs — cold INPUT, not a cold index (R30)
- [Order-sensitive Vision-buffer consumers — OPEN](memories/order-sensitive-vision-buffer-consumers-open.md) — flagged not fixed (R36): TargetingSystem/KnnProjectileCollisionSystem/DefensePlatformDefenseSystem pick a KnnVision entry by raw buffer position (Vision[0]/Vision[^1]/first-qualifying) with no tiebreak, peer-divergent on co-located ties; UnitOverlapPreventionSystem's key-sorted consumption is the pattern to copy — a driver+Ben design item, not a drive-by fix
- [Silent host rejects look like a dead ability](memories/silent-host-rejects-look-like-a-dead-ability.md) — Bat frenzy "almost never works" was the host silently rejecting casts: the 055 T017 request-only conversion carried Plasma's per-player cooldown gate onto all four ability kinds, so the UI's per-ship-derived charge prediction promised more than the host allowed (fixed `f4e3af6a2`); when converting a local action to a request/apply pair, keep the ORIGINAL gate owner (per-ship vs per-player), and diagnose via the actor's saved `DeterministicAbilityFireState`, not the validation code
- [A request/apply conversion must keep the release owner too](memories/request-apply-conversion-must-keep-the-release-owner-too.md) — the generic applier stamps `DeterministicAbilityRelease = Target + Duration`, right for Guardian/Frenzy whose Duration IS the hold, wrong for the Obliterator whose Duration is the disc's FLIGHT lifetime (073 T030): stamping it would have freed the Krillos before the 2 s charge and deleted the disc — converted, deterministic, dead; read what ENDS the effect today and cite it; every-peer spawn recipe inside (prefab by name, deferred ECB entity as a component reference)
- [A "does not resolve" log from saved history is not a fork](memories/a-does-not-resolve-log-from-saved-history-is-not-a-fork.md) — g15's identical `Commanded ship object id 720 … does not resolve` on both peers was a stale `[Save]`d row: the per-player ability intent/commanded-ship buffers were never pruned (52 intents + 92 rows on hazel1831's host, 996 on its partner), rows carry no kind, and per-kind sequence lanes let a new Obliterator cast walk old Frenzy rows for a dead Bat (073 T035, fixed `27df24bed`: unique-per-actor sequences + prune at apply); a deterministic error on BOTH peers is evidence about shared STATE — load the same save solo, dump the buffers, find the id in the world census before listing per-peer mechanisms; ask "who removes from this buffer?" of anything `[Save]`d
- [Wire-surface adds have THREE mirrors](memories/wire-surface-adds-have-three-mirrors.md) — the text Fingerprint line, `determinism_audit_verdict.py` `FINGERPRINT_ORDER`, and the JSON verification projection (`NetworkDeterminismAuditVerificationSample`) all need updating; 055 T055 missed the third one and every verification report for a week silently failed the strict cross-machine verdict while the text-channel sweeps stayed green
- [A player's transform is presentation — resolve every player range/hit/aggro decision via DeterministicEntityPosition](memories/player-presentation-transform-forks-combat.md) — 2026-09-12: OldBeamShootingSystem read the player's LocalTransform for its beam range gate → BeamDamageSystem ticked 0.165 hp/s on the host only → `combat` fork, re-fork at hb 8 after every recovery, Steam kick; fixed 4aabc5d4a. RETRACTED the same day: the projectile-on-remote-player KNN path is NOT this class — the player's KNN presence is its PlayerSimulationObject at the simulation position
- [Burst OnCreate + a new SystemAPI lookup = NRE in the Burst-compiled OnUpdate](memories/burst-oncreate-plus-new-systemapi-lookup-nre.md) — EditMode tests NRE under Burst, pass managed, pass on the unmodified system; JIT wipe and the Burst toggle do not help; drop `[BurstCompile]` from `OnCreate`
- [Map-gen regime must match across peers](memories/map-gen-regime-must-match-across-peers.md) — `MapGenerationData`/`DisableEnemyGeneration` are peer-local and unserialized; a saveload arm that configures FlatMap/enemy-generation differently on host vs client forks on whichever peer's terrain rolls a camp, several heartbeats before any fingerprint surface shows it
- [LinkedEntityGroup dangling entries and clone aliasing](memories/linkedentitygroup-dangling-entry-and-clone-aliasing.md) — a linked child destroyed without unlinking makes the parent's next Instantiate a Burst abort ("srcEntity's LinkedEntityGroup references an entity that is invalid"); Instantiate remaps only the source's OWN group so an unlinked nested child is SHARED by the clone; an unlink-now/destroy-later window lets a later system re-link the dying set; integrity-scan probe + the BlueprintPreviewGroupHygieneSystem tripwire (069 objective-13 crash, 47a909dfb)

## Localization

- [French percent = Arabic sign](memories/french-percent-arabic-sign.md) — Unity's Mono culture data gives fr `PercentSymbol` = U+066A ٪ (no font has the glyph); fixed by CultureSetup.cs, plus the Mono Clone()-shares-NumberFormatInfo gotcha
- [TextLocalization duplicate gotcha](memories/textlocalization-duplicate-gotcha.md) — TextLocalization caches live TMP text as its key, so duplicating it on a label (Checkbox prefabs already have it) locks that label to one language; never put it on proper-noun labels

## Testing & tooling gotchas

- [Native build success with errors](memories/build-report-success-with-errors.md) — require a zero-error BuildReport, C#/Burst checks, baked SubScene files, and real startup; CleanBuildCache recovered an observed missing-scene build
- [Baseline before lifecycle tests](memories/live-baseline-before-lifecycle.md) — preserve both paired reports before reconnect, and verify temporal capture framing
- [Mirror implementations need a golden fixture](memories/mirror-implementation-golden-fixture.md) — a report-only set (or any logic) reimplemented in more than one language (C#/shell/python) drifts silently without a shared golden fixture pinned in every mirror, plus a test that they agree
- [Hand-rolled test double must mirror or say so](memories/hand-rolled-test-double-must-mirror-or-say-so.md) — a test double that deliberately reproduces a production shape (055 R37 KnnLateSpawnTransformPassWitnessTest mirroring AttackingShipSpawnerSystem) must name the real-system test covering the actual code path, or the mirror can go vacuous silently when either side changes
- [`cmd | tee log || true` clobbers PIPESTATUS](memories/shell-tee-pipestatus.md) — the exit code is always 0 regardless of `cmd`'s real result; use `set +e`/`set -e` around the pipe instead
- [Long tool sessions and archive verification](memories/tool-session-polling-and-archive-verification.md) — forward nested terminal results to expose `session_id`, poll with `write_stdin`, and treat outer-cell output as non-verdict; archive proof compares independently regenerated raw-tar hashes plus full inventories and manifests
- [Guard tests follow the landed fix, not the proposed one](memories/guard-tests-follow-the-landed-fix-not-the-proposed-one.md) — a guard written alongside a proposed fix design encodes that design's invariant (055 R37: a writer-side guard would have failed the reader-side fix that actually landed at `7304103d6`); re-derive from what shipped, and treat a census guard's unreviewed count as the deliverable to adjudicate, not noise to silence
- [Actual-system handoff tests follow source group order](memories/actual-system-handoff-test-order.md) — when a handoff crosses fixed groups, preseed the prior actual state, drive the real handoff/playback, and observe before the next producer pass; never insert an impossible producer pass to satisfy an assertion
- [EnterWorktree cuts from master, not develop](memories/enterworktree-cuts-from-master-not-develop.md) — the default `worktree.baseRef=fresh` branches from `origin/master`, but Final Factory integrates on `develop`, so a fresh worktree is missing every in-flight spec's files; first command in any new worktree is `git reset --hard origin/develop`, verified with `git log -1`
- [Audit fixtures and report grounding](memories/audit-fixture-and-report-grounding.md) — source-count agreement never replaces the full fingerprint comparator; a join fixture must create construction tasks before asserting its witness; inspect baked prefab components before assigning a loader fault; normalize only extractor line endings and keep raw report bytes; require natural player exits and working fixture topology; initialized components, output filters and bounded charge witnesses matter; event cleanup must preserve pending heartbeat work and future command-buffer events; Mac app-root runtime status, startup readiness and actual restore witnesses.
- [Cross-machine built-player gameplay acceptance](memories/cross-machine-built-player-gameplay-acceptance.md) — (the gate behind Ben's standing rule above) a multiplayer gameplay fix requires matching current built players on two physical machines, Windows/macOS when affected, the reported sequence through real inputs, both-peer screenshots actually inspected, temporal evidence for motion, and an unweakened all-25-field typed comparison over the whole shared heartbeat window; direct IP does not prove Steam paths, and Windows GUI players launched over SSH may need a task-scoped launch into the logged-in desktop session.
- [Live tutorial evidence and producer tests](memories/live-tutorial-evidence-and-producer-tests.md) — run the tutorial to completion without a fixed time cutoff, retain continuous and live evidence, test actual scheduled producers, and preserve reports through atomic verified transfer.
- [Built-player screenshot and pointer coordinates](memories/built-player-screenshot-coordinate-scale.md) — screenshot response dimensions can be downscaled and are not raw pointer coordinates; read the live native view before driving UI, and clear both preview entities and native blueprint allocations (live-verified fix).
- [Built-player native CUA input](memories/built-player-native-cua-input.md) — on the Mac player, choose the exact app path, re-query AX state, raise its current window with native CUA, and inspect fresh AgentControl previews; separate repeated keys by game frames; real `R` input is distinct from networked `rotate.abs` and forbidden local RED injection.
- [Built-player boot hang and harness waiters](memories/built-player-hang-and-harness-waiters.md) — a shell-launched player can stall at waiting-title-menu at 0.2% CPU (kill the path-verified PID, relaunch); the Claude harness kills run_in_background sleep-pollers under memory pressure, so long waits go through Monitor
- [The verdict script rejects whole reports on eviction/give-up records](memories/verdict-script-rejects-eviction-records.md) — `continuous_determinism_verdict.py` scans the WHOLE file, not the window, and re-checks the footer counters; compare a run that ended in a network drop by dropping exactly the records its own `reject_unhealthy_record()` rejects, correcting the footer, listing every dropped line, and ending closed-epoch windows one sample early; a forced resync is NOT unhealthy
- [The `asteroids` fingerprint is blind to depleted/untargeted asteroids](memories/asteroids-fingerprint-blind-to-depleted-asteroids.md) — it hashes only asteroids a station targets or a miner sources, so an exhausted asteroid leaves the set exactly while its deletion timing forks; proven live (073 f4-cp1: 16,776 shared hb, 0 mismatches, while the peers disagreed for ~1,100 hb on whether it existed). A green hash over a KNOWN entity-set change is a blind-spot finding — poll the entity set directly, then widen the owning surface; widened at `4dc66303e` and LIVE-PROVEN positive 2026-09-16 (h1: `asteroids` the only mismatching field at the deletion heartbeat, detector verdict + recovery)
- [Multiplayer-dev player builds from the unity CLI shim](memories/unity-cli-mpdev-build-recipe.md) — no `--json` flag; `filter_type=testName|assembly|category`; call `BuildAllMultiplayerDevNoUpload` by reflection in a guarded update callback (the platform entry points skip localization + cicd copy); the no-version-update path leaves `PlayerSettings.bundleVersion` stale — eval it first; a platform-switch build can take ~140 min on shader-graph reimport; never `rebase --autostash` mid-build
- [Judge a build by its marker and child processes, not the editor's CPU](memories/judge-a-build-by-marker-and-children-not-editor-cpu.md) — during a build both editor channels time out and the editor process can sit at ~1% (Bee/IL2CPP are children): marker mtime + `pgrep -P <editor pid>` + Editor.log growth are the signals; the log may have rotated to `Editor-prev.log` when another editor started

## Gameplay diagnosis & live-test recipes

- [Snapshot recovery rebinds live input callbacks](memories/recovery-rebinds-input-callbacks.md) — reusing PlayerDataController/StateActions during snapshot apply repeats SetupController; detach exact publishers before rebind and on destruction, then prove clicks through resync → reconnect → resync
- [Unified 16 UPS smooth presentation](memories/project-unified-16ups-smooth-presentation.md) — feature 057's goal: one 16 UPS baseline for SP and MP with per-frame presentation, and the simulation/presentation boundary that must never be crossed; read the feature plan's latest handoff for where the program stands
- [Player domain is already conventional](memories/player-domain-already-conventional.md) — the player replication/simulation boundary already matches the proposed split; presentation rate is the real issue, while combat remains the open domain decision
- [Cargo ship teleport diagnosis](memories/cargo-ship-teleport-diagnosis.md) — camera-gated presentation sync over ungated simulation = ghost-then-snap artifact (InserterRenderSystem); plus the live-probe/A-B methodology via execute_code (onBeforeRender probes, GravityForces mover, Error-Pause gotcha)
- [Mobile station merge bug](memories/mobile-station-merge-bug.md) — why stations merged on landing (overlap-only checks vs EntityMap, flying stations absent from the map); fixed via landing-footprint claims (TryClaimLandingZone, landed on master); bug-report saves load by filename via SaveGameManager.LoadGame
- [Black hole visual test recipe](memories/blackhole-visual-test-recipe.md) — "BlackHole" save + teleport coords, 200u death radius, swirl = rotating skybox (the shader never animates), fixed-res Game view screenshot workaround
- [Tutorial objectives 76–78 from the harness](memories/tutorial-map-fleet-complete-automation.md) — ui.open|map then ui.close|map (MapUsed ticks on CLOSE), ui.open|fleet, and the final Complete button is a LOCAL click on every peer (pointer coords inside)
- [Real-terrain automation rejoin](memories/automation-realmap-rejoin.md) — `net.rejoin` defaults to FlatMap; real-terrain replay uses `|45|realmap`, lets the verb perform its own leave, and judges the restored session rather than an HTTP-chain cancellation
- [MoversDetail group diffs and the float mirror flicker](memories/movers-detail-group-diff-and-float-mirror-flicker.md) — diff v/h/d/f/m/p/r + raw vel/hd by key first; the cross-platform movers flicker is float ULP noise CombatMoverRailMirrorSystem quantizes from LinearMotion.Velocity into the compared rail; BEAST-host + M5-editor-client leg recipe
- [Derived-on-load grid state must ride the save](memories/derived-on-load-grid-state-must-ride-the-save.md) — the load-time regrid of FFGrid.EntityMap is not idempotent (circle re-claim, irregular bit-buffer origin/tail-shift mismatch, first-wins); persist the ownership (EntityGridPersistenceSystem) and overlay after the regrid pass; probe host reports from their LAST epoch-1 block
- [Cross-platform leg staging and the movers position residual](memories/cross-platform-leg-staging-and-the-movers-position-residual.md) — tar.gz+scp+make_manifest on BEAST, the nohup launch form that starts, and the one-ULP Position flips left after mirrored Velocity/Heading stopped folding (Ben's scope call)
- [Objective code runs per peer — use the op queue](memories/objective-code-runs-per-peer-use-the-op-queue.md) — tutorial triggers/rewards run on engine frames per peer; a camp spawn or `[Save]` research write there forks the peers by exactly ONE heartbeat at an objective boundary (run-2 vision verdict hb 7936); route through host-authored outbound ops (58/59, ae0623198)
- [Headless Windows player over SSH, pointer mapping, placement route](memories/headless-windows-player-over-ssh-and-placement-route.md) — BEAST has no desktop so its player is headless (no pointer/ui, ability objectives never tick) → M5 windowed player hosts; native = 2× the 1280×691 shot with Y flipped; inventory→ghost placement route + its three blockers; `AuditSaveName generated:<seed>`; `movement.goto` is WORLD units; `PYTHONIOENCODING=utf-8`; the client's only desync signals
- [Mac Steam upload keeps asking for login = desktop-app vs steamcmd session conflict](memories/steamcmd-vs-desktop-steam-login-conflict.md) — NOT a bad password: the Steam desktop app and brew steamcmd share account slims20 and Steam allows one session per account, so they evict each other; a fresh steamcmd login says "Cached credentials not found" regardless of desktop state. Quit the desktop app, then log in AND upload in ONE steamcmd command; or upload from BEAST. Full recipe: [[steam-upload]] skill.
- [Triage a Steam desync from the client side only](memories/steam-desync-triage-from-the-client-side-only.md) — Ben 2026-09-12: never use the other player's machine; what Player.log carries, dating both builds (depot DLL byte-diff, system type names inside the transfer save, what the join gate does NOT cover), and the reproduction rule: the host's OWN player under fire
- [Diagnostic-profile capture config that actually launches](memories/diagnostic-profile-config-recipe.md) — verification-v2 schema, ushort window, positive limits, finite dwell, ≥1 surface; detail rows are plain [DeterminismAudit] lines; Fingerprint every heartbeat; the verdict script rejects windows containing the host's recovery record — diff Fingerprint fields directly
- [Fleet harness facts from the desync reproduction (2026-09-12)](memories/fleet-harness-operational-2026-09-12.md) — BEAST go-scripts must run in the ssh foreground; saved-player identity is per machine (M5 = Ben's, BEAST/M3 = Loth's); player.setposition refused after join; combat.respawn; fresh Bats never engage, patrol through the camp draws fire
- [Built-client relaunch and evidence traps](memories/built-client-relaunch-and-evidence-traps.md) — a menu-state client refuses every ffauto command (409 state_not_playable): relaunch, never `net.rejoin`; a relaunch with the SAME AuditRunId/LegId cannot auto-write its report over the previous artifact (move it first — the r10b epoch-14 verdict lost its client fingerprints this way); lane signal counters fail closed (NA, never empty; `grep -c` exits 1 on zero); LAN-IP joins failed (`MaxConnectionAttempts`) while Tailscale joins work; a climbing epoch with 0 recoveries = link flaps
- [The HUD health-bar chain forks the census after a join](memories/health-bar-chain-forks-the-census-on-join.md) — HealthBarInitializer/HealthBarReference/HealthBar are per-peer "hit observed" presentation, never saved, so a joined/recovered peer lacks them for 6–9 hb (074 T106, fixed b577625f3); the 10-minute single-player per-heartbeat census-dump probe; hb-0-only load transients that must NOT be stripped
- [Kicked clients write their audit report — fetch it](memories/kicked-clients-write-their-audit-report.md) — cp3.sh cannot reach a kicked peer; the auto-written `network-determinism-audit-<runId>-<legId>-client.log` + `fielddiff.py` per epoch separated one double kick into three defects (1-hb apply residual, permanent post-recovery fork, survivor fork on a peer kick)
- [Pumped execute_code scripts can wedge the editor](memories/pumped-execute-code-scripts-can-wedge-the-editor.md) — long Step() pumps after LoadGame (waituntil polling, a 60-s until-running loop) spun the main thread: `Command TCS timed out`, stale heartbeat, CLI hung too; keep calls < 40 s / ≤ 300 Steps, poll ECS directly, sample the main thread, then path-verified kill + `scripts/launch-editor.sh` + OpenScene
- [Built-pair lab traps, day 3 (073 g10/g11)](memories/built-pair-lab-traps-2026-09-18.md) — macOS sed has no `\b` (leg scripts silently kept the old leg's files); scp to Windows OpenSSH needs `C:/Users/…` targets; `snapshot/nearby` takes TILE coords; PowerShell Select-String is case-insensitive (`BC[0-9]{4}` matched a cache hash — use -CaseSensitive); `PowerSatisfactionDetail.nCalc` is per peer, mask it; the kicked client's auto-written report under LocalLow/DeterminismAudit replaces a checkpoint it can no longer serve; day 4: `construction.place` says "placed" even when the ghost never commits (16-tile finder range + 200-unit action range), `desync.inject` = a one-second forced recovery, a fork kicked at hb 13 is not "permanent", `game.save` works solo; day 6: BEAST's `wincredman` credential store makes `git fetch` HANG silently (use `GIT_TERMINAL_PROMPT=0` to see it) so the bundle is mandatory and needs a REF range, and copying the universal-binary player to the other arm64 Mac beats two independent builds
- [A determinism PASS must be proven non-vacuous](memories/determinism-pass-must-be-proven-non-vacuous.md) — compare the distinct fingerprint-hash POPULATION before vs after the action on BOTH peers (a run that placed nothing reports the same clean verdict as a fix); and never write an acceptance criterion requiring an ephemeral entity to show up in `observe.state` — the census is the only instrument that sees it
- [Review findings need parent adjudication](memories/review-findings-need-parent-adjudication.md) — a high-effort review called a REQUIRED build define stale residue to revert (`BuildMultiplayerDev` throws without it) and read `ControllerSystems/` as a Controller group when the attribute says `FFFixedPreTransformGroup`; re-open every cited line, most of all on findings that say revert or delete
- [Editor-pair per-heartbeat census hook](memories/editor-pair-per-heartbeat-census-hook.md) — an `EditorApplication.update` hook on both editors dumps `CollectCensusRecords` one TSV per heartbeat of every join/recovery epoch (survives play-mode entry: domain reload on play is off); pair by exact heartbeat and the leaking row names itself — captures post-recovery epochs the CensusDetail anchor never can; named 074 T108 in one leg
- [A stale clone editor invalidates pair evidence](memories/stale-clone-editor-invalidates-pair-evidence.md) — the clone kept its pre-fix `FFSpaghetti.dll` and showed the pre-fix census fork; prove the fix TYPE exists on BOTH editors (`Type.GetType` + DLL mtime), `refresh_unity scope=all mode=force` (scope=scripts rebuilt nothing), preflight until Burst drains.
- [Unbuilt-site load-shape probe recipe](memories/unbuilt-site-load-shape-probe-recipe.md) — an itemless `construction.place` ghost stays a `frame` forever (a cbot beside the player builds a real one in 9 hb); save it, resume as editor host + join a clone, one `execute_code` entity probe per editor; comets are never marshalled (074 T110).
- [The loader re-instantiates the prefab: baked `[Save]` components the save omits come back](memories/loader-reinstantiates-prefab-baked-save-components-return.md) — `SaveGameManager` only ADDS missing saved components after `Instantiate(itemPrefab)`, so any marker the live path strips returns on every load and every loading peer forks against the creator (074 T108: `KnnFleetEntity` on an unbuilt site, fixed `168f37837` by a restoration-sequence helper system); grep the live creation path for `RemoveComponent<That>`
- [Built-pair lab traps (073)](memories/built-pair-lab-traps-073.md) — `snapshot/nearby` asteroid poll is BLIND at r=8, use r>=40 (prove it goes positive first); `cpcompare.py` hardcodes `--players 2` (fpcompare.py is count-agnostic); a BEAST poller launched `nohup … &` inside a one-shot ssh dies silently — run it in the ssh FOREGROUND of a locally backgrounded ssh; never put `|` in a plain ssh→cmd.exe line; BEAST git needs a bundle over scp, not a fetch
- [The `census` fingerprint surface: design + how to diagnose a red](memories/census-fingerprint-surface-design.md) — game component types only, engine types never, `Disabled` only beside a game type; the `CensusDetail` diagnostic dump + per-heartbeat row diff names the leaking kind; found the comet wall-clock spawner, the revealer skew and the recovery reset's `Comet` leftover on day one; diagnostic limits must fit int32
- [The single-player reset probe for recovery leftovers](memories/single-player-reset-probe-for-recovery-leftovers.md) — load through the DevLoadSave hook, census by reflection on `DeterminismStateFingerprintJobs.CollectCensusRecords`, `ResetGame()`, census, reload, census: survivors that GROW on the reload are the post-recovery fork (073 T024: 24 -> 48 arrows). The loaded game sits in editor pause, so pump `Step()`.
- [Built-pair structure removal + the early diagnostic checkpoint](memories/built-pair-structure-removal-and-early-diagnostic-checkpoint.md) — `construction.remove` resolves only the ground tile and needs construction bots + the player in action range; `endedHeartbeat < startedHeartbeat` = a recovery crossed the chain; block on the first `DesyncRecoveryAttempt` then `cp.sh` at once (2-minute CensusDetail iterations); a "same shape" leg can differ silently (windowed host: the beam mined nothing); hazel1831 commander inventory
- [The bridge drops the main instance on every EditMode run](memories/bridge-drops-main-instance-during-editmode-runs.md) — "instance not found" right after `run_tests`/a play-mode exit is the reload, not a wedge: wait ~45 s, re-read instances (port can flip), poll the SAME job id. With two editors, this project's log is `Editor-prev.log`; during a long suite it drops repeatedly and a blip can answer `Unknown job_id` for a job that still finishes — wait for the status file fresh + port open, re-pin by PORT, poll the same id in a later turn; an `execute_code` longer than ~2 min times out on the bridge while the editor keeps running it (judge by marker).
- [Prove a widened instrument positive with a revert pair](memories/revert-pair-proves-a-widened-instrument.md) — build the pair from `<tip> + revert(<fix>)` so the fork is live and the new surface must name it (073 h1: `asteroids` at hb 1198, detector verdict, recovery); scratch commit through a temporary `GIT_INDEX_FILE` (live editor tree untouched), inverse-diff verification, bundle to BEAST via PowerShell (`&&` dies in a plain ssh→cmd.exe line like `|`), `scope=all` refresh + reflection proof around the Mac build; `cpcompare.py` `evidence-invalid` on a recovery window is expected
- [Agent-control chains are one FIFO per player; `mining.startnearest` is the player's beam](memories/agent-control-chain-fifo-and-mining-startnearest.md) — a throttle/wait segment holds the whole queue (the checkpoint's `audit.write` waited 15 min behind a throttle and looked like a missing report; no cancel route) — actions → throttle LAST → checkpoint after; `mining.startnearest|<range>` needs a Mineable target within <range> WORLD units of SimulationPosition (`|400` from two tiles away), and always read the chain result — the earlier legs' mining segment had silently failed
- [The player-inventory arrangement diverges by design; chase totals, not slots](memories/player-inventory-arrangement-diverges-by-design.md) — `playerInventories` (slot arrangement) diverges as soon as the local player's inventory changes (`PlayerInventoryUpdateNotifier` sorts only the local peer's OWN player), is exempt, and only gets counted; `playerInvTotals` must agree. It cannot fork `containers` through an InventoryTransfer (every slot folded; the saturating leftover depends on a permutation-invariant fit sum) — a pickup fork needs the acting player's TOTALS to differ. `transfer.grab` uses the panel API but is Primary-only, omits `SlotIndex`, and needs a positive count; use actual inventory totals to confirm movement
- [`net.dropreconnect` reconnects at once, and its disconnect record poisons the verdict](memories/dropreconnect-verb-and-injected-disconnect-verdict-record.md) — the N-second argument only suppresses the connection probe; the engine restores at attempt 1 in ~2 s for N=10 and N=60 alike (a real flap needs a firewall rule); the chain returns `cancelled after 0 of 1 segment(s)` (not a failure); the host's `AuditLifecycle phase=error` dwell-gate record makes the verdict script `evidence-invalid` — filter it like an eviction
- [The session reset applies at its stream position; two-peer lanes cannot see the backlog fork](memories/session-reset-marker-and-the-three-peer-backlog-fork.md) — a join/recovery reset used to clear every peer's queues at receipt, so a lagging THIRD peer dropped its un-applied backlog and forked at the first sample (cascade: 3 recoveries from one resync); fixed 1bb8a9cb1 with the `SessionReset` marker (non-served peers apply the reset in stream order, host waits for its marker before the snapshot, sampler skips while pending); any desync class needing a non-served client needs a 3-peer lane
- [Three-peer lane recipe and traps](memories/three-peer-lane-recipe-and-traps.md) — M3 host + M5 client A + the clone editor as trigger B; `AuditSourceRevision` must be the full 40-hex sha; ordered start host→`host-ready`→A→`client-ready`→then flip the clone's `AutoStartInEditor` (the poller starts play + `ResetForEditorLaunch`; manual `EnterPlaymode` stalls); `flowcontrol.stall` returns only after the delay (background it); the clone's `player-safe` actor cannot `audit.write`; `cpcompare.py` hardcodes `--players 2`; host/A must share a legId
- [The host audit label defers to its marker; filter pre-marker records from pre-fix reports](memories/host-audit-label-defers-to-the-marker-and-the-premarker-filter.md) — `AdvanceForReset` no longer sets the label (`c939c0858`); a pre-fix host report shows "hb 741 followed by 1" under one epoch (`evidence-invalid`) — drop that epoch's per-heartbeat records before the counter's last backwards step by record sequence, print every drop, correct the footer.
- [The shim's test-name filter does not narrow; the status file can be stale; statics leak between runs](memories/unity-shim-run-tests-filter-does-not-narrow-and-static-test-isolation.md) — `--filter X --filter_type testName` ran all 4036; parse the shim JSON `result.Results[].Status`, never grep "Total" or trust `Temp/pipeline_test_status.json`; `HeartbeatSystem._lastAppliedSessionResetEpoch` needed a `SetUp` clear (`2306524df`); judge from ONE fresh-domain run.
- [Placement and grid attachment from the harness (r15)](memories/placement-and-grid-attachment-from-the-harness-r15.md) — GridTile = lower-left; a Solar Panel joins only via its direct-connection slot (place Up one left/one below, `rotate.abs` 3 then 1 — the anchor moves); `ui.click` never proves arming (screenshot 2-3 s after `pointer.moveto`); a stray built panel is refunded; bot paths are not structures; `nearby` radius ≥ 20 for asteroids.
- [M3 scratch cleanup: delete named roots, never a wildcard sweep](memories/m3-scratch-sweep-refused-delete-named-roots.md) — `rm -rf /private/tmp/ff-*` over ssh is refused by the permission classifier; enumerate, pick closed roots by feature/sha, delete by name (sixteen `ff-*-011*` → 8.8 GB free).

- [Material-property overrides fork the census (074 T105)](memories/material-property-overrides-fork-the-census.md) — the placing peer's ghost preview instantiates the LogisticsRouteDisplay prefab whose children carry only `[MaterialProperty]` components; remote and recovered peers never get it → permanent `census` fork from the first `construction.place`. Any type with `Unity.Rendering.MaterialPropertyAttribute` is stripped now; T103's per-load report singletons are the sibling. Diagnose with `fielddiff.py` over the cp reports + a single-player `CollectCensusRecords` before/after probe, no diagnostic leg
- [Client autosave → pause → watchdog reconnect (074 T101)](memories/client-autosave-watchdog-reconnect.md) — every MP client autosaved ~5 min in, NRE'd, froze, `LivenessWatchdog` reconnected it; saving is host-authority (`IsSaveAuthorityPeer`, `game.save` answers `host-only` on a client)
- [Accepted casts log nothing; objective counters are presentation hooks (074 T104)](memories/accepted-casts-log-nothing-objective-counters-are-presentation-hooks.md) — `[PlayerAbilityFire]` lines are rejects only; prove an apply from `DeterministicAbilityFireState.LastAppliedEventSequence` or the ships' components; `FrenzyUsed`/… ride `AbilityNotifierService.OnAbilityUsed`, now raised once in `PlayerAbilityFireDispatch.Dispatch`
- [New-game audit legs, the diagnostic anchor latch, post-recovery evidence (074)](memories/new-game-audit-leg-config-and-the-diagnostic-anchor-latch.md) — a new-game leg declares `AuditSaveName: seed:<seed>` + sha256(seed); the diagnostic anchor closes at the next block (arm after the LAST join); a recovery makes `cp3.sh` `evidence-invalid` — diff the per-hb `Fingerprint.fields` directly

- [Preview descendants leak into the census](memories/preview-descendants-leak-into-census.md) — inspect the live parent chain before stripping a component; the held printer preview adds two RotationParameters children, while real Krillo claws must remain covered.
- [Continuous capture for long acceptance](memories/continuous-capture-for-long-acceptance.md) — verification-v3-continuous journals beyond the v2 16384 cap and avoids legacy ring rows rejected by the strict parser; require a live typed baseline.

## Modding

- [Mod ABI package pinning](memories/mod-abi-package-pinning.md) — why Entities/URP versions are pinned (entities 1.3.10 / URP 17 on Unity 6000.0.71f1)
- [ModLoader metadata inspection](memories/modloader-metadata-inspection.md) — mods inspected reflection-only (MetadataLoadContext) before Assembly.Load; which 3 plugin DLLs, the 4 polyfills to skip, and the FFCore.Unity namespace-shadowing gotcha (use global::Unity.Entities)
- [Mod template breaks on new global-namespace game types](memories/mod-template-global-debug-autoreference.md) — FFCore's global `Debug` class produced ~260 CS0576 errors in the template's Unity packages via DLL auto-reference; fixed with tracked Auto-Reference-off metas + explicit precompiledReferences; check the template after shipping global types

## ffbox pipeline & ffweb

- [`Referrer-Policy: no-referrer` nulls your own Origin](memories/referrer-policy-nulls-your-own-origin.md) — a page that sets no-referrer AND checks `Origin` for CSRF refuses its own forms (Fetch serialises the origin as `null` on a non-GET, non-CORS request); use `same-origin`, accept `Sec-Fetch-Site: same-origin`, and note that a suite posting with no Origin header never catches it
- [A Claude result envelope's `subtype` is not a verdict](memories/claude-result-subtype-is-not-a-verdict.md) — the last `{"type":"result"}` record keeps `subtype: "success"` on a run it also flags `is_error`, and its `result` field carries the error text instead of an answer; read `is_error`, then `terminal_reason`/`api_error_status`, and treat a subtype of `success` as no detail at all. Reading it the obvious way told pull request 505 "the run failed: success" and put `success` in `turn.error`

- [An ffbox container CAN run Unity and play the game](memories/ffbox-containers-can-run-unity.md) — there are no per-lane Bash allow lists; every run gets bare `Bash`, so `unity-editor` is directly runnable and only the git/gh tripwire is denied. `ffplaytest` is the play-mode wrapper (config deleted on every exit path, editor killed as a process group, label-scoped journal); it needs a DEVELOP-based workspace and its frame timings are meaningless (no GPU). When a capability changes, fix every prompt that describes it in the same commit

- [UnityMCP works headless in an ffbox container](memories/unitymcp-works-headless-in-a-container.md) — 46 tools are served to the packaged server and `read_console` answers live from a batchmode editor. Needs four things: the PyPI `mcpforunityserver` baked into the image via uv at BUILD time (then it runs under `--network none`, so the egress fence needs nothing new), `UNITY_MCP_ALLOW_BATCH=1` (the bridge returns early in batchmode without it), `-executeMethod MCPForUnity.Editor.McpCiBoot.StartStdioForCi` with no `-quit`, and killing the editor as a PROCESS GROUP. Readiness is the log line `StdioBridgeHost started on port N`, NOT the port registry file, which is only written when the default port is taken. LIVE for ffdev on ffbox since 2026-09-12: 78-83s to a bridge, 863/863 EditMode in ~57s vs ffverify's 226s, 0 files dirtied by a whole turn. `No Unity Editor instances found` usually means BUSY (mid assembly-reload after run_tests; `wait_timeout` does not absorb it) or a `$HOME` mismatch — never assume the editor died

## Maintaining this skill

New durable lessons go here (one file in `memories/`, one index line above, in the topical
section it belongs to), committed to the final-factory-agents repo — NOT to the per-worktree
`~/.claude/projects/*/memory/` dirs, which are machine-local and do not propagate.
- [Audit capture policies must be symmetric across peers](memories/audit-capture-policy-must-be-symmetric.md) — a host-only diagnostic profile is refused at pairing (`IsSymmetricWith`, "Audit capture policies differ"); the CensusDetail block that launched three peers (074 t8), ~435 KB/s host log, dwell expiry THROWS, the anchor closes at the FIRST recovery, `cp3.sh` says `evidence-invalid` on such a leg — `fielddiff.py` per epoch is the evidence
- [The comet detector, and why the naive one is swamped](memories/comet-detector-and-the-swamped-census-change-detector.md) — no ffauto verb or snapshot scope sees a comet; symmetric CensusDetail + `cometwatch.sh` (byte-offset tail for a `FFComponents.Map.Comet[],]` row, proven positive first) caught one for the T110 proof; "census changed on an idle world" fires on ~50 % of heartbeats; spawn `CoinFlip(0.005*dt)` per hb, life 960 hb
- [Marshalled-entity-type precedence Player > Item > Comet](memories/marshalled-entity-type-precedence-player-item-comet.md) — a new `AsteroEntityType` claims only `Invalid` entities in BOTH capture paths (legacy loop + columnar), in any type order; the 055 fixtures build comets ON item ids and load through the item prefab — the first cut dropped them ("expected 2, was 0"); missing prefab must be LOUD
- [Parameter-tail edits hit two methods; judge a compile by Csc marker order](memories/parameter-tail-edits-hit-two-methods-and-csc-marker-order.md) — `CreateGroupEntities`/`WriteGroupData` share a parameter tail (CS7036); `grep -c` the new symbol after any scripted signature edit; a failed compile keeps the last-good DLL while the log tail shows OLD errors — read only after the last Csc marker, then DLL mtime + `Type.GetType` on every editor
